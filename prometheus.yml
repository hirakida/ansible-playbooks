---
- name: Prometheus
  hosts: all
  become: true
  vars:
  roles:
    - role: prometheus
      tags: prometheus
    - role: prometheus_alertmanager
      tags: alertmanager
    - role: prometheus_pushgateway
      tags: pushgateway
